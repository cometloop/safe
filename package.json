{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "concurrently -n \"turbo,stripe\" \"turbo run dev\" \"stripe listen --forward-to http://localhost:4000/api/auth/stripe/webhook\"",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.7.3"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "with-vite-react",
  "pnpm": {
    "overrides": {
      "vite": "^7.3.1",
      "@types/react": "^19.2.0",
      "@types/react-dom": "^19.2.0",
      "csstype": "^3.2.3"
    }
  }
}
